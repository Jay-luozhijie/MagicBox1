<div class="collapse" id="comment">
    <%if(currentUser){%>
        <form action="/<%=idea._id%>/comment" class="validated-form mb-3 mx-5" id="commentForm" method="POST" novalidate>
            <div class="my-3">
                <label class="form-label" for="commentbody">Comment</label>
                <textarea class='form-control' name='comment' id='commentbody' cols='30' rows='2'></textarea>
            </div>
            <div class="d-flex flex-row-reverse">
                <button class="btn btn-sm commentSubmit">Submit</button>
            </div>
        </form>
        <hr style="height:20px; color:#ced4da;">
    <%}%>
    <div>
        <div class="mx-5" id="commentsContainer">
           
        </div>
        <div id='commentNavigation' class='d-flex justify-content-center'>
            
        </div>
    </div>
</div>

<script>
    const c = '<%-JSON.stringify(currentUser)%>'
    let currentUser = undefined
    let commentAuthor = undefined
    if('<%-JSON.stringify(currentUser)%>'){
        currentUser = JSON.parse('<%-JSON.stringify(currentUser)%>')
        commentAuthor = currentUser.username
    }
    const ideaId = '<%=idea._id%>'
    const commentArrayLength='<%=idea.comment.length%>'
</script>
<script src="https://code.jquery.com/jquery-3.6.0.js" integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk=" crossorigin="anonymous"></script>
<script src='/js/comment/submitComment.js'></script>
<script src='js/comment/paginatedComment.js'></script>
<script src='js/comment/submitReply.js'></script>
